source "https://rubygems.org"

gemspec

# Uncomment to work on the Lakehouse::Database (SQL endpoint) path locally. Requires the
# Microsoft ODBC Driver 18 (msodbcsql18) installed on the host.
# gem "ruby-odbc", require: "odbc"
